{
  "private": false,
  "name": "hdr-prefilter-texture",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --sw false --no-prerender --esm false",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch --sw false --no-prerender --esm false",
    "lint": "eslint src"
  },
  "homepage": "https://deepkolos.github.io/hdr-prefilter-texture/index.html",
  "author": {
    "name": "DeepKolos",
    "url": "https://github.com/deepkolos"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^7.17.0",
    "eslint-config-preact": "^1.1.3",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "three-platformize": "^1.0.6"
  }
}
